# Sample Data
age <- c(25, 30, 22, 35, 28)
score <- c(4, 5, 3, 4, 5)


plot(age, score,
     main = "Age vs Satisfaction Score",
     xlab = "Age",
     ylab = "Satisfaction Score",
     pch = 19,
     col = "blue")