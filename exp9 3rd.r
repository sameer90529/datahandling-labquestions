survey_data <- data.frame(
  Survey_ID = c(1, 2, 3),
  Question1 = c("A", "B", "C"),
  Question2 = c("B", "A", "A"),
  Question3 = c("C", "D", "B")
)

# Display Dataset
print(survey_data)

print(survey_data)

# View table in RStudio
View(survey_data)
